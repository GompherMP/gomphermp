#align(center)[
  #text(12pt, weight: "bold")[Resumen]
]

#pad(x: 2em)[
  #set par(justify: true)
  Este documento define la arquitectura de la herramienta GompherMP. Se detallan los módulos, sus interacciones, el flujo general del programa y los detalles de la transformación de código.
]
